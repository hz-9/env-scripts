- Makefile 测试目标遵循统一的参数校验与日志记录模式：检查必需变量（ENV/SCRIPT/FILE），缺失时输出用法提示并退出；运行时创建 `logs/` 目录并以时间戳生成日志文件。
- 测试命令参数通过 `ARGS` 变量动态组装，并根据 Make 变量（NETWORK、DEBUG、OUTPUT、DOCKER_IMAGE_QUICK_CHECK）的存在性追加对应标志，最后统一传递给 `test-environment-manager.sh`。
- 所有 Make 目标在执行前后输出带颜色与分隔线的状态提示（🚀 开始 / ✅ 完成），并在完成后打印日志文件路径，保持交互体验一致。