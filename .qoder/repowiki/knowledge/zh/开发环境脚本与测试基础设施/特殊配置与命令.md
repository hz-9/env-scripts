- 初始化：执行 `make build` 可同时构建脚本产物与 Docker 镜像。
- 运行测试：使用 `make install-test-all` 或 `make syncdb-test-single ENV=ubuntu22-04 SCRIPT=mysql` 等目标触发测试，支持 `NETWORK=in-china`、`DEBUG=true`、`OUTPUT=path` 等参数。
- 清理与调试：`make clean` 清理容器与镜像，`make shell` 进入指定容器调试，`make results` 查看测试日志。